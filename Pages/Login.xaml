<Page
    x:Class="App1.Pages.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock x:Name="pageTitle" Text="登录" />
        <HyperlinkButton Content="返回" 
                 Click="HyperlinkButton_Click"
                 HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="欢迎登录" Margin="0,0,10,0"/>
                <TextBlock Text="{x:Bind LoginInfo.UserName, Mode=OneWay}"/>
            </StackPanel>
            <TextBox x:Name="usernameBox" Header="输入用户名:" Width="500" Text="{x:Bind LoginInfo.UserName, Mode=TwoWay}"/>
            <PasswordBox x:Name="passwordBox" Header="输入密码:" Width="500"/>
            <Button x:Name="login" Content="登录" Click="login_Click"/>
        <TextBlock x:Name="text" Text=""/>

        </StackPanel>
    </Grid>
</Page>
